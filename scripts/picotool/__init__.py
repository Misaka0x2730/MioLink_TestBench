"""picotool helper modules.

  - load: flash a firmware image onto an RP2040/RP2350 device that is
    already in BOOTSEL mode, using picotool over the PICOBOOT vendor
    interface (acknowledged + verifiable writes, deterministic reboot).
"""
